# Argo CD Application: public-api (PostGraphile) + chain-event-handlers for mainnet (eu main cluster).
# Namespace: cfg-event-lake. Sync wave 1 (after Redpanda).
# query runs Drizzle migrations at boot; handlers wait for PUBLIC_API_HEALTH_URL then replay Kafka.
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: public-api-mainnet
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  project: cfg-api
  source:
    repoURL: https://embrio-tech.github.io/centrifuge-helm
    targetRevision: "0.1.0"
    chart: centrifuge-public-api
    helm:
      releaseName: public-api-mainnet
      values: |
        global:
          env:
            CFG_NETWORK: "mainnet"
            REDPANDA_BROKERS: "redpanda:9093"
        query:
          image:
            tag: "<PINNED_TAG>"
          ingress:
            enabled: true
            hosts:
              - host: api.cfg.embrio.tech
                paths:
                  - path: /
                    pathType: Prefix
            tls:
              - secretName: api-cfg-embrio-tech-tls
                hosts:
                  - api.cfg.embrio.tech
        handlers:
          image:
            tag: "<PINNED_TAG>"
        postgres:
          enabled: true
          cluster:
            storage:
              storageClass: ceph-perf3
  destination:
    server: https://kubernetes.default.svc
    namespace: cfg-event-lake
