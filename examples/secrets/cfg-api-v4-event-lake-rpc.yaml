# kubectl -n cfg-api create secret generic cfg-api-v4-event-lake-rpc-overrides \
#   --from-literal=ENVIO_API_TOKEN='<token>'
# Extra keys (optional): --from-literal=ENVIO_RPC_URL_<chainId>=http://...
#
# Empty ENVIO_API_TOKEN is treated as unset. Envio HyperSync requires a real token;
# without one the indexer falls back to RPC-only (in-cluster eRPC).
#
# Default RPC URLs are generated from erpc.urlTemplate. If used, set
# global.apiSecretName to this Secret's metadata.name. Keys here override
# ConfigMap ENVIO_RPC_URL_* values.
#
# apiVersion: v1
# kind: Secret
# metadata:
#   name: cfg-api-v4-event-lake-rpc-overrides
#   namespace: cfg-api
# type: Opaque
# stringData:
#   ENVIO_API_TOKEN: "<token>"
