# Optional. Only if you need to override in-cluster eRPC or set ENVIO_API_TOKEN.
# Default RPC URLs are generated from erpc.urlTemplate (same-cluster eRPC).
#
# If used, set global.apiSecretName to this Secret's metadata.name. Keys here
# override ConfigMap ENVIO_RPC_URL_* values.
#
# apiVersion: v1
# kind: Secret
# metadata:
#   name: cfg-api-v4-event-lake-rpc-overrides
#   namespace: cfg-api
# type: Opaque
# stringData:
#   ENVIO_API_TOKEN: ""
