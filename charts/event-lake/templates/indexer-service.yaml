{{- if .Values.indexer.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "centrifuge-event-lake.indexer.fullname" . }}
  labels:
    {{- include "centrifuge-event-lake.indexer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.indexer.service.type }}
  ports:
    - port: {{ .Values.indexer.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "centrifuge-event-lake.indexer.selectorLabels" . | nindent 4 }}
{{- end }}
