{{- if .Values.bootstrap.topics.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "centrifuge-redpanda.fullname" . }}-bootstrap-topics
  labels:
    {{- include "centrifuge-redpanda.labels" . | nindent 4 }}
    app.kubernetes.io/component: bootstrap-topics
  annotations:
    "helm.sh/hook": post-install,post-upgrade
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
    "helm.sh/hook-weight": "0"
spec:
  backoffLimit: 3
  activeDeadlineSeconds: {{ add (int (.Values.bootstrap.wait.timeoutSeconds | default 300)) 120 }}
  template:
    metadata:
      labels:
        {{- include "centrifuge-redpanda.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: bootstrap-topics
    spec:
      restartPolicy: Never
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: rpk
          image: {{ include "centrifuge-redpanda.topicsImage" . | quote }}
          imagePullPolicy: {{ .Values.bootstrap.topics.pullPolicy | default "IfNotPresent" }}
          {{- with .Values.bootstrap.topics.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          env:
            - name: RPK_BROKERS
              value: {{ include "centrifuge-redpanda.brokers" . | quote }}
          command:
            - /bin/sh
            - -c
            - |
              set -eu
              TIMEOUT={{ int (.Values.bootstrap.wait.timeoutSeconds | default 300) }}
              INTERVAL={{ int (.Values.bootstrap.wait.intervalSeconds | default 5) }}
              deadline=$(( $(date +%s) + TIMEOUT ))
              echo "waiting for Kafka metadata at ${RPK_BROKERS} (timeout ${TIMEOUT}s)"
              # cluster health uses Admin API (localhost:9644 by default) and
              # ignores --brokers. cluster info is the Kafka metadata call.
              until rpk cluster info >"/tmp/rpk.out" 2>"/tmp/rpk.err"; do
                if [ "$(date +%s)" -ge "$deadline" ]; then
                  echo "Redpanda did not answer Kafka metadata within ${TIMEOUT}s"
                  cat "/tmp/rpk.err" || true
                  rpk cluster info || true
                  exit 1
                fi
                echo "not ready: $(tr '\n' ' ' < /tmp/rpk.err)"
                sleep "$INTERVAL"
              done
              echo "Kafka reachable; reconciling topics"
              for NET in {{ range .Values.bootstrap.topics.networks }}{{ . }} {{ end }}; do
                for T in "${NET}.protocol-events.speculative" "${NET}.protocol-events.confirmed" "${NET}.protocol-events-dlq"; do
                  if rpk topic describe "$T" >/dev/null 2>&1; then
                    echo "exists ${T}"
                    continue
                  fi
                  EXTRA=""
                  case "$T" in *speculative) EXTRA="-c cleanup.policy=compact";; esac
                  # shellcheck disable=SC2086
                  rpk topic create "$T" -p {{ .Values.bootstrap.topics.partitions }} -r {{ .Values.bootstrap.topics.replicas }} $EXTRA
                  echo "created ${T}"
                done
              done
{{- end }}
