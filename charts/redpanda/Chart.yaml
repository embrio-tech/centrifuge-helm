apiVersion: v2
name: redpanda
description: Redpanda cluster (Kafka API + Schema Registry + Console) for Centrifuge protocol events, with topic and schema bootstrap
type: application
version: 0.1.6
appVersion: "v26.1.14"
dependencies:
  - name: redpanda
    repository: https://charts.redpanda.com
    version: 26.1.10
